python-dotenv>=1.0.0
aiohttp>=3.9.0
pynacl>=1.5.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# --- 충돌 해결 필요 (별도 설치) ---
# nado-protocol: eth-account <0.9.0 요구
# grvt-pysdk:    eth-account >=0.13.4 요구
# 아래 순서대로 설치 후 python fix_deps.py 실행:
#   pip install grvt-pysdk>=0.2.0
#   pip install nado-protocol>=0.1.0 --no-deps
#   python fix_deps.py
